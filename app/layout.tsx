import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Agent Memory Kit — Persistent Memory for Your AI Agent',
  description: 'The complete setup to build persistent, searchable memory for your OpenClaw agent. Scripts, configs, templates, and a step-by-step guide.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
